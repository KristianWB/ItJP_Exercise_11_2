public class Student extends Person {

    // Defining variables
    String status;

    // Constructing student object
    Student()   {
        status = "#null#";
    }

    // Constructing defined student object
    Student(String newStatus)   {
        status = newStatus;
    }
}
